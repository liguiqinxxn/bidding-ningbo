name: CI/CD Pipeline
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  # 代码检查
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-node@v4
      with:
        node-version: lts/*
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Run ESLint
      run: npm run lint

    - name: Run Stylelint
      run: npm run stylelint

  # 单元测试
  unit-test:
    runs-on: ubuntu-latest
    needs: lint
    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-node@v4
      with:
        node-version: lts/*
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Run unit tests with coverage
      run: npm run test:coverage

    - name: Check coverage thresholds
      run: |
        # 读取 JSON 报告
        if [ ! -f "coverage/coverage-summary.json" ]; then
          echo "❌ Coverage report not found!"
          exit 1
        fi

        # 提取覆盖率数据
        STATEMENTS=$(node -e "console.log(JSON.parse(require('fs').readFileSync('coverage/coverage-summary.json', 'utf8')).total.statements.pct)")
        BRANCHES=$(node -e "console.log(JSON.parse(require('fs').readFileSync('coverage/coverage-summary.json', 'utf8')).total.branches.pct)")
        FUNCTIONS=$(node -e "console.log(JSON.parse(require('fs').readFileSync('coverage/coverage-summary.json', 'utf8')).total.functions.pct)")
        LINES=$(node -e "console.log(JSON.parse(require('fs').readFileSync('coverage/coverage-summary.json', 'utf8')).total.lines.pct)")

        # 定义阈值
        MIN_STATEMENTS=80
        MIN_BRANCHES=75
        MIN_FUNCTIONS=80
        MIN_LINES=80

        echo "📊 Coverage Results:"
        echo "  Statements: $STATEMENTS% (min $MIN_STATEMENTS%)"
        echo "  Branches:   $BRANCHES% (min $MIN_BRANCHES%)"
        echo "  Functions:  $FUNCTIONS% (min $MIN_FUNCTIONS%)"
        echo "  Lines:      $LINES% (min $MIN_LINES%)"

        # 检查是否达标
        failed=false
        [[ $(echo "$STATEMENTS < $MIN_STATEMENTS" | bc -l) == "1" ]] && echo "❌ Statements below threshold!" && failed=true
        [[ $(echo "$BRANCHES < $MIN_BRANCHES" | bc -l) == "1" ]] && echo "❌ Branches below threshold!" && failed=true
        [[ $(echo "$FUNCTIONS < $MIN_FUNCTIONS" | bc -l) == "1" ]] && echo "❌ Functions below threshold!" && failed=true
        [[ $(echo "$LINES < $MIN_LINES" | bc -l) == "1" ]] && echo "❌ Lines below threshold!" && failed=true

        if [ "$failed" = true ]; then
          echo "💥 Coverage threshold not met!"
          exit 1
        else
          echo "✅ All coverage thresholds met!"
        fi

  # 组件测试
  component-test:
    runs-on: ubuntu-latest
    needs: lint
    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-node@v4
      with:
        node-version: lts/*
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Run component tests
      run: npm run test:run

  # E2E测试
  e2e-test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    needs: lint
    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-node@v4
      with:
        node-version: lts/*
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Install Playwright Browsers
      run: npx playwright install --with-deps

    - name: Run Playwright tests
      run: npx playwright test

    - uses: actions/upload-artifact@v4
      if: always()
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 30